<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error</title>
    <link rel="stylesheet" href="/css/output.css"> <%/* Link to Tailwind output */%>
    <%/* Remove or comment out old stylesheet links */%>
    <%/* <link rel="stylesheet" href="/css/style.css"> */%>
    <%/* Remove the inline <style> block if you plan to replace it with Tailwind classes */%>
</head>
<body>
    <%/* Start using Tailwind classes here! */%>
    <div class="container mx-auto mt-10 max-w-xl p-6 border border-red-600 rounded-lg bg-red-100 text-red-800 shadow-md">
        <h1 class="text-2xl font-bold text-red-800 mb-4 text-center">An Error Occurred</h1>

        <% if (typeof message !== 'undefined' && message) { %>
            <p class="text-lg mb-4"><%= message %></p>
        <% } else { %>
            <p class="text-lg mb-4">An unexpected error occurred. Please try again later or contact support.</p>
        <% } %>

        <div class="text-center">
             <a href="/" class="text-blue-600 hover:text-blue-800 font-semibold">Go back to Home Page</a>
        </div>
    </div>
</body>
</html>