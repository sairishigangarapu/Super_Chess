<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 text-white">
    <main class="min-h-screen flex items-center justify-center p-4">
        <div class="bg-zinc-800 p-8 rounded-lg shadow-2xl max-w-md w-full">
            <h1 class="text-2xl font-bold mb-6">Login</h1>
            <form action="/login" method="POST" class="space-y-4">
                <div>
                    <label for="email" class="block text-zinc-400 mb-1">Email</label>
                    <input type="email" id="email" name="email" required class="w-full bg-zinc-700 text-white px-4 py-2 rounded">
                </div>
                <div>
                    <label for="password" class="block text-zinc-400 mb-1">Password</label>
                    <input type="password" id="password" name="password" required class="w-full bg-zinc-700 text-white px-4 py-2 rounded">
                </div>
                <button type="submit" class="w-full bg-blue-600 text-white py-2 rounded hover:bg-blue-700">Login</button>
            </form>
            <p class="text-center text-zinc-400 mt-4">
                Don't have an account? <a href="/register" class="text-blue-400 hover:text-blue-300">Create one</a>
            </p>
        </div>
    </main>
</body>
</html>