<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Spectate Matches - Chess Game</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 text-white">
    <div class="min-h-screen p-10">
        <div class="flex justify-between items-center mb-8">
            <h1 class="text-3xl font-bold">Spectate Matches</h1>
            <a href="/dashboard" class="bg-zinc-800 hover:bg-zinc-700 text-white px-4 py-2 rounded-lg">Back to Dashboard</a>
        </div>
        <div class="max-w-2xl mx-auto">
            <% if (activeGames.length === 0) { %>
                <p class="text-lg text-zinc-400">No games are happening right now. Check back later!</p>
            <% } else { %>
                <div class="space-y-4">
                    <% activeGames.forEach(game => { %>
                        <div class="flex items-center justify-between p-4 bg-zinc-800 rounded-lg">
                            <p class="text-lg"><%= game.white %> vs <%= game.black %></p>
                            <a href="/spectate/<%= game.gameId %>" class="bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded">Spectate</a>
                        </div>
                    <% }) %>
                </div>
            <% } %>
        </div>
    </div>
</body>
</html>